services:
  - type: web
    name: deal-neural-engine
    env: node
    plan: pro
    region: frankfurt
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PROJECT_ROOT
        value: DALABS_HARMONY
      - key: AI_CORE
        value: G_AGI_INDEPENDENT
