services:
  - type: web
    name: deal-protocol-brain
    env: node
    region: oregon
    plan: free
    buildCommand: "cd core_brain && npm install"
    startCommand: "cd core_brain && node server.js"
    envVars:
      - key: NODE_ENV
        value: production
