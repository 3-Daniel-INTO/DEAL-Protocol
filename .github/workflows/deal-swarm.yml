name: DEAL Swarm Deployment
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: DEAL Security Scan (G-AGI)
        run: |
          echo ">> [G-AGI]: Escaneando integridad cuántica de la red..."
          npm install
          npm run build --if-present
